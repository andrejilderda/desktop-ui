import ThemeConsumer from '../ThemeConsumer';

describe('ThemeConsumer', () => {
  test.todo('An error is thrown when used outside a ThemeProvider');
  test.todo('Props are begin forwarded properly');
});
